import NavLinks from "./NavLinks";

const Header = () => {
  return (
    <header className="header">
      <p className="logo" width="5%"/>
      <NavLinks />
    </header>
  );
};

export default Header;
